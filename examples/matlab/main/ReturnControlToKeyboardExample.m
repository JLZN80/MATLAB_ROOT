%% Return Control to Keyboard
% In your current working folder, create a function, |findSqrRootIndex|, to
% find the index of the first occurrence of the square root of a value
% within an array. If the square root is not found, the function returns
% |NaN|.
% 
% <include>findSqrRootIndex.m</include>
%
% At the command prompt, call the function.
A = [3 7 28 14 42 9 0];
b = 81;
findSqrRootIndex(b,A)
%%
% When MATLAB encounters the return statement, it returns control to the
% keyboard because there is no invoking script or function.

%% 
% Copyright 2012 The MathWorks, Inc.